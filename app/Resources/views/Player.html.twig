{% extends "::layout.html.twig" %}
{% block content %}
<h3>Rejestracja</h3>
<p>
    Nazwa Gracza <br>
{{ form_start(form) }} {# wyświetla znacznik <form> z odpowiednio uzupełnionymi danymi #}
    {{ form_widget(form.name) }} {# renderuje widget ("surową" część html-ową) naszego formularza, np pole tekstowe #}
    {{ form_widget(form) }} {# renderuje wszystkie pozostałe pola formularza #}
<button type="submit">Rejestracja</button>
    {{ form_end(form) }} {# renderuje wszystkie pozostałe pola formularza oraz zamyka znacznik form otwarty przez funkcję form_start #}

</p>
{% endblock %}